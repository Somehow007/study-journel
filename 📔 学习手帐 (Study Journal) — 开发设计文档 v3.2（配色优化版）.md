## 📔 学习手帐 (Study Journal) — 开发设计文档 v3.2（配色优化版）

---

### 目录
1. 项目概述
2. 核心功能模块
3. 页面布局与交互设计（含完整低保真线框图）
4. 视觉与情绪设计（柔彩果冻治愈风 · 设计令牌体系 · 双模式）
5. 动效与微交互规范
6. 加载动画设计
7. 空状态页面设计
8. 响应式设计概览
9. 数据模型与存储
10. 技术栈选型
11. 开发路线图
12. 附录

---

### 1. 项目概述

#### 1.1 项目名称
**学习手帐 (Study Journal)** — 一个注重 UI 美感与舒适记录体验的个人学习日记与心情追踪 Web 应用。

#### 1.2 核心定位
**数字手帐**：记录每天学什么、心情如何、有什么想法。回顾过去像翻阅一本有温度的时间之书。

#### 1.3 核心痛点与解决方案
- **痛点**：已有番茄钟等计时工具，但缺少美观、有温度、重回顾的学习与心情日记。
- **解决**：不做计时器，手动填写学习内容与时长。设计优先，注重记录与回顾的愉悦感。

#### 1.4 关键设计原则
- 不做计时器/番茄钟，不与现有工具重叠
- 手动填写学习内容与时长，关注记录与回顾，而非实时计时
- UI 设计优先：治愈、精致、不费力，提供翻看回忆的愉悦感
- 便捷访问：Web 应用，打开即用，本地存储，零数据上传，绝对隐私
- 仪式感：打开应用的瞬间像翻开一本真正的日记本

---

### 2. 核心功能模块

#### 2.1 每日心情记录
- 选择当天的心情（预设 6 种：开心、平静、低落、灵感、焦虑、疲惫）
- 每种心情对应专属图标与主色调
- 伴随轻量动画反馈，心情影响当日卡片主色与页面氛围

#### 2.2 学习记录
- 添加多条学习记录（学科/项目名称、时长小时+分钟、简短备注）
- 卡片式展示，每条记录带可自定义颜色标记点
- 支持滑动删除、点击编辑
- 底部汇总当日总学习时长

#### 2.3 日记/想法记录
- 自由书写区域，支持简单 Markdown 排版（加粗、列表、标题）
- 无标题栏设计，鼓励随手记录
- 自动保存至本地存储，右下角显示保存状态指示

#### 2.4 月视图日历（首页）
- 完整日历网格显示每日状态卡片
- 每张卡片包含：日期数字、心情图标、学习时长进度条
- 悬浮卡片显示当天简记与心情 tooltip
- 快速切换月份，一键回到"今天"
- 今日卡片有特殊光晕标记

#### 2.5 回忆与统计
- **心情画廊**：以月为单位的颜色矩阵（GitHub 贡献图风格），回顾情绪起伏
- **时间轴视图**：按时间流展示过往每日记录，像翻阅社交媒体动态
- **学习统计**：轻量图表（每日学习时长柱状图、本月学科分布环形图）

#### 2.6 本地数据管理
- 所有数据存储于浏览器 IndexedDB，离线可用，无后端
- 支持 JSON 格式数据导出/导入备份
- 未来可扩展云同步（P2 优先级）

---

### 3. 页面布局与交互设计

#### 3.1 整体结构

桌面端采用**左侧固定导航 + 右侧内容区**布局，最大内容宽度 960px，居中有呼吸感。侧栏顶部是 Logo 与应用名，中部是主导航（今日、月历、回忆、统计），底部是设置/关于/导出入口。右侧主内容区根据导航切换。

```
┌──────────────┐  ┌─────────────────────────────────────────────┐
│   📔  Logo   │  │                                             │
│   · 手帐     │  │         主内容区 (右侧)                     │
│              │  │                                             │
│   ○ 今日     │  │   (根据导航切换：月视图 / 今日详情 / 回忆)  │
│   ○ 月历     │  │                                             │
│   ○ 回忆     │  │                                             │
│   ○ 统计     │  │                                             │
│              │  │                                             │
│  ──────────  │  │                                             │
│  设置/关于   │  │                                             │
└──────────────┘  └─────────────────────────────────────────────┘
```

左侧是固定导航，右侧是核心区域。

#### 3.2 月视图日历（首页）

打开网站看到的画面，核心是**心情 + 学习的卡片日历**。

**顶部导航栏**：
- 左侧：年份 + 月份标题（手写体，如"2026年 7月"）
- 右侧：◀ 上月 | 今天按钮（胶囊形虚线边框）| 下月 ▶
- 标题下方有细装饰线分隔

**日历网格**：
- 7 列 × 5-6 行标准日历布局
- 顶部显示星期标题（日 一 二 三 四 五 六）
- 每个日期单元格为独立卡片，有心情记录时背景使用心情软底色（浅色模式 `soft`，深色模式 `softDark`）+ 边框 `1px solid var(--color-line)`

**日期卡片内容**（80px × 80px，间距 8px，圆角 `radius-lg` 16px，半透明奶白底 + `backdrop-filter: blur(12px)` 毛玻璃）：
- 右上角：日期数字（`text-sm`，次要文字色）
- 中央：心情 Emoji/图标（28px），有记录时卡片底部叠加心情渐变光晕（`mood-glow`）
- 底部：学习时长进度条（高 4px，圆角 `radius-full`，使用心情渐变色 `linear-gradient(90deg, 渐变亮, 渐变暗)`，宽度按 0-8h 比例映射，无记录时显示浅灰虚线 `rgba(189,178,168,0.3)`）

**低保真线框图**：

```
┌─────────────────────────────────────────────────────────────┐
│  2026年 7月                            ◀  ▶  今天           │
├─────────────────────────────────────────────────────────────┤
│  日   一   二   三   四   五   六                           │
│              1    2    3    4    5                           │
│  ┌──┐ ┌──┐ ┌──┐ ┌──┐ ┌──┐ ┌──┐                            │
│  │☀️│ │☁️│ │📚│ │💻│ │💤│ │😊│  ← 心情图标+学习时长小环   │
│  │  │ │  │ │▮▮│ │▮ │ │  │ │▮▮│                            │
│  └──┘ └──┘ └──┘ └──┘ └──┘ └──┘                            │
│  6    7    8    9    10   11   12                           │
│  ┌──┐ ┌──┐ ┌──┐ ┌──┐ ┌──┐ ┌──┐                            │
│  │📖│ │✨│ │☕│ │📚│ │😴│ │💪│                             │
│  │▮ │ │▮▮│ │  │ │▮▮│ │  │ │▮▮│                            │
│  └──┘ └──┘ └──┘ └──┘ └──┘ └──┘                            │
│  ... (继续)                                                │
│                                                             │
│  图例：☀️开心 ☁️平静 🌧️低落 ⚡焦虑 💡灵感 😴疲惫          │
│        底部细条长度 = 学习时长 (0-8h)                       │
└─────────────────────────────────────────────────────────────┘
```

**悬浮态**：
- 卡片放大至 105%
- 阴影从 `shadow-2` 升至 `shadow-3`
- 卡片叠加 `mood-glow`（当日心情光晕色），边缘柔光晕染
- 上方弹出 tooltip 气泡（`radius-xl`，`shadow-3`，毛玻璃底）：显示学科 + 时长 + 心情简记

**今日标记**：
- 卡片外围蜂蜜金 `mood-glow`（`0 0 24px rgba(255,185,56,0.20)`）
- 像被阳光温柔点亮，深色模式下光晕更亮更像小夜灯

**交互**：
- 点击任意日期卡片 → 进入该日详情页
- 左右箭头切换月份
- "今天"按钮回到当前日期

#### 3.3 今日详情页（日记画布）

点击某一天后的视图，可以记录或查看当天的所有内容。

**低保真线框图**：

```
┌─────────────────────────────────────────────────────────────┐
│  ← 返回月历                    2026年7月5日 星期六          │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  今天心情如何？                                             │
│  ┌──────────────────────────────────────────────────────┐  │
│  │  😊       ☁️       🌧️       ⚡       💡       😴      │  │
│  │ (开心)  (平静)  (低落)  (焦虑)  (灵感)  (疲惫)      │  │
│  └──────────────────────────────────────────────────────┘  │
│  (选中状态：有温暖底色和放大动画)                            │
│                                                             │
│ ── 今日学习 ──────────────────────────── [+ 添加记录] ──  │
│  ┌─────────────────────┐  ┌─────────────────────┐         │
│  │ ● React 组件设计    │  │ ● 算法题 (2道)      │         │
│  │   2 小时 30 分钟    │  │   1 小时 15 分钟     │         │
│  │   理解了 context..  │  │   有点卡壳，明天复习 │         │
│  └─────────────────────┘  └─────────────────────┘         │
│  (卡片可滑动删除或编辑)                                     │
│                                                             │
│ ── 今日想法 ──────────────────────────────────────────────  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 今天下雨，在窗边看书很舒服。                          │   │
│  │ 发现一个超好用的状态管理库……                         │   │
│  │                                                     │   │
│  │ (支持简单的 Markdown，自动保存)                      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│                                         [保存]  自动已保存🟢 │
└─────────────────────────────────────────────────────────────┘
```

**顶部**：
- 返回箭头按钮（← 返回月历）
- 日期显示：完整日期 + 手写体星期（如"2026年7月5日 星期六"）

**心情选择器**：
- 六个圆形图标卡片水平排列（72px 直径，间距 `space-3`，圆角 `radius-full`）
- 默认态：透明背景，灰色图标，虚线边框（`rgba(189,178,168,0.3)`）
- 悬浮态：背景渐变为该心情软底色（浅色模式用 `soft`，深色模式用 `softDark`）+ `mood-glow` 柔光晕，图标恢复原色
- 选中态：背景变为心情渐变色（`linear-gradient(160deg, 渐变亮, 渐变暗)`）+ `jelly-inner` 内高光，图标变白，伴随果冻弹性缩放动画，周围扩散波纹光晕
- 选中后页面顶部区域渐变晕染该心情色，像水彩洇开。浅色模式用 soft 色 35% 透明度（`hexToRgba(soft, 0.35)`），深色模式用 main 色 8% 透明度（`hexToRgba(main, 0.08)`）做夜灯微光，避免浅色色块在暗背景上遮文字
- 交互细节：点一下即选中，伴随果冻回弹动画（scale 0.85→1.08→1.0），背景色随心情变

**学习记录区**：
- 标题"今日学习"（`text-xl`）+ [+ 添加记录] 虚线胶囊按钮（`radius-full`，虚线色 `rgba(189,178,168,0.4)`）
- 已添加记录以卡片展示（`radius-lg`，半透明奶白底 + 毛玻璃，`shadow-2`，左侧 4px 心情渐变竖线，学科名 `text-lg` 加粗 + 时长等宽字体标签 + 备注次要文字色）
- 卡片悬浮升至 `shadow-3` + 微抬起 2px，间距 `space-3`
- 卡片可滑动删除或点击编辑
- 底部汇总当日总学习时长（等宽字体，`text-lg`）
- 添加弹窗：底部毛玻璃面板滑入（`radius-2xl` + `shadow-4`），表单包含学科/项目名、时长(小时+分钟)、备注，确认后卡片果冻弹入

**日记区**：
- 无边框大文本区域，背景带隐约横线（浅色 `rgba(200,190,180,0.3)` / 深色 `rgba(120,110,100,0.15)`，类似信纸）
- 左侧装饰性心情渐变竖线（4px 宽）
- 字体 Inter `text-base` 16px，行高 2，颜色 `#2D2620`（浅色）/ `#F5EDDA`（深色）
- 占位文字"随手写点什么… 今天窗外是什么天气？"（手写体 Caveat，`#948A80` / 深色 `#8A7F75`）
- 支持简单 Markdown 渲染（加粗、列表、标题）
- 右下角自动保存指示："🟢 已保存"（`text-xs`，淡绿文字）
- 底部保存状态指示：手动保存 + 自动保存双重保险

#### 3.4 回忆与统计页

这是"看过去自己"的地方。顶部标签切换：`[画廊] [时间轴] [统计]`。

##### 3.4.1 画廊模式（默认）

以月为单位的颜色矩阵，像 GitHub 贡献图，但颜色代表心情。

**低保真线框图**：

```
┌─────────────────────────────────────────────────────────────┐
│  回忆 · 2026年7月              [画廊] [时间轴] [统计]       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  心情矩阵 (像 GitHub 贡献图, 但颜色代表心情)                │
│       一 二 三 四 五 六 日                                   │
│  第1周 🟡🔵🔵🟡🟢🟢🔵                                      │
│  第2周 🔵🔵🟡🟡🔵🟣🟣    🟡开心 🟢平静 🔵低落 🟣灵感       │
│  第3周 🟢🟢🟡🟡🔵🔵🔵                                      │
│  第4周 ...                                                  │
│                                                             │
│  点击某一格 → 下方显示当天日记摘要和学习总时长              │
│  ┌──────────────────────────────────────┐                   │
│  │ 7月12日 · 平静                       │                   │
│  │ 学习 4.5 小时  "今天把项目部署上线"  │                   │
│  └──────────────────────────────────────┘                   │
└─────────────────────────────────────────────────────────────┘
```

- 月份选择器
- 心情贡献矩阵：每个小格 32px × 32px，圆角 `radius-sm` 8px，间距 `space-1` 4px
- 有记录：填充心情渐变色（`linear-gradient(160deg, 渐变亮, 渐变暗)`）+ 顶部内高光；无记录：虚线边框空白格（`rgba(189,178,168,0.25)` 虚线）
- 悬浮：格子放大 120% + `mood-glow` 光晕 + 弹出详情气泡（`radius-xl` 毛玻璃，`shadow-3`：日期 + 心情 + 学习时长）
- 底部回忆卡片：`radius-xl` + `shadow-2` + 毛玻璃底，显示选中日期完整摘要（心情图标 + 日记摘录 + 学习标签），卡片左侧有心情渐变竖线

##### 3.4.2 时间轴视图

按时间流展示过往每日记录，像翻阅社交媒体动态。

**低保真线框图**：

```
┌─────────────────────────────────────────────────────────────┐
│  回忆流                                                     │
│ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─│
│  7月5日 · 😊 开心                                           │
│  ┌──────────────────────────────────────┐                   │
│  │ 📚 学 React    2.5h                  │                   │
│  │ 💡 灵感笔记："状态提升原来这么简单"  │                   │
│  └──────────────────────────────────────┘                   │
│ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─│
│  7月4日 · ☁️ 平静                                           │
│  ┌──────────────────────────────────────┐                   │
│  │ 📖 读《设计模式》 1h                 │                   │
│  │ ☕ 休息日，只看了半小时书             │                   │
│  └──────────────────────────────────────┘                   │
│  (无限滚动，带淡入动效)                                     │
└─────────────────────────────────────────────────────────────┘
```

- 垂直中线设计（`#D5CCC0` 浅色 / `#4A443C` 深色，2px 宽，带渐变淡出上下端）
- 日期卡片左右交替排列（或统一左侧，保持简洁），中线节点为心情色小圆点（带 `mood-glow`）
- 每条记录卡片：`radius-lg` + `shadow-2` + 毛玻璃底，日期 + 心情 emoji 顶部，背景使用心情软底色微渐变，学习标签 + 日记前几行
- 悬浮卡片微微横移 4px + 升至 `shadow-3`，带淡入动效
- 无限滚动加载过往记录

##### 3.4.3 统计模式

轻量图表展示学习数据。

**低保真线框图**：

```
学习时长统计 (本月)
┌───────────────────────────┐
│ 每日学习时长柱状图        │
│ ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓    │
│ (recharts 轻量图表)       │
│                           │
│ 学科分布                  │
│ React    ████████ 52%    │
│ 算法      ██████   30%   │
│ 英语      ███      18%   │
└───────────────────────────┘
```

- 每日学习时长柱状图：柱子使用蜂蜜金渐变（`linear-gradient(180deg, #FFD66B, #FFA51F)`），圆角 `radius-sm` 顶部，悬浮升至 `shadow-3` + 显示具体数值和日期
- 学科分布环形图：每段使用对应学科标记色，中心显示总学习时长（等宽字体 `text-xl`）
- 月度总览摘要：三张小卡片横排（`radius-lg` + `shadow-1` + 毛玻璃），分别显示总学习天数、总时长、最常用心情

#### 3.5 添加学习记录弹窗

**低保真线框图**：

```
  ┌──────────────────────────┐
  │  添加学习记录          ✕ │
  ├──────────────────────────┤
  │  项目/学科 [___________] │
  │  时长      [__] 小时 [__] 分钟 │
  │  备注      [___________] │
  │                          │
  │         [取消]  [添加]   │
  └──────────────────────────┘
```

- 从底部毛玻璃面板滑入（300ms，`ease-out`）
- 表单字段：项目/学科名称、时长（小时 + 分钟）、简短备注
- 学科标记点：预设 8 种颜色小圆点供用户选择
- 确认后卡片以飞入动画进入学习记录区
- 移动端改为底部抽屉式全宽

---

### 4. 视觉与情绪设计

#### 4.1 整体氛围基调

**设计风格**：柔彩果冻治愈风（Soft Jelly Aesthetic）。在保留手帐温度感的同时，引入微渐变、内发光、半透明质感，让界面像水果软糖一样有弹性、有光泽、想戳一下。

**关键词**：柔彩、果冻、微渐变、内发光、呼吸感、半透明

**视觉哲学**：
- **不是平面，是果冻**：卡片有微微的 3D 质感——顶部高光、底部微影，像光打在凝胶上
- **不是纯色，是微渐变**：所有大色块都使用 3-5% 色差的双色渐变，肉眼几乎察觉不到但潜意识觉得"有层次"
- **不是硬边界，是柔光晕**：重要元素靠 outer glow 而非 border 来吸引注意
- **不是灰蒙蒙，是甜而不腻**：颜色饱和度比莫兰迪高 30%，但靠半透明和留白控制甜度

**背景体系**：
- 页面背景使用三色微渐变（奶纸 → 暖桃 → 奶杏），叠加极淡噪点纹理增加纸质感
- 卡片背景使用半透明白色 + `backdrop-filter: blur()` 毛玻璃效果，让底层渐变隐约透出
- 深色模式下背景变为深暖棕渐变（非纯黑），保持温度感

#### 4.2 心情色板系统（柔彩果冻色）

每种心情不再是一个扁平色块，而是一组包含主色、渐变对、柔光晕、软底色的完整令牌。选中态使用渐变 + 内高光模拟果冻质感，未选中态仅显示柔光晕。

**浅色模式（Light）**：

| 心情 | 图标 | 风格名     | 主色       | 渐变（亮→暗）                 | 软底色     | 深色软底色  | 光晕色                     |
| ---- | ---- | ---------- | ---------- | ----------------------------- | ---------- | ---------- | --------------------------- |
| 开心 | 😊    | 蜂蜜金果冻 | `#FFB938`  | `#FFD66B → #FFA51F`           | `#FFEAB0`  | `#3A2F1A`  | `rgba(255,185,56,0.20)`     |
| 平静 | ☁️    | 天空果冻   | `#4DB8E5`  | `#6ECBF5 → #38A5D8`           | `#C5E8F8`  | `#1A2C35`  | `rgba(77,184,229,0.20)`     |
| 低落 | 🌧️    | 薰衣草果冻 | `#9B7AD9`  | `#B394E8 → #8360CA`           | `#D5C5F5`  | `#2A2238`  | `rgba(155,122,217,0.20)`    |
| 灵感 | 💡    | 花瓣果冻   | `#E876C4`  | `#F59AD4 → #D458B0`           | `#F2C8E8`  | `#321E2B`  | `rgba(232,118,196,0.20)`    |
| 焦虑 | ⚡    | 珊瑚果冻   | `#FF7E5C`  | `#FF9C7E → #F56040`           | `#FFCDBF`  | `#34211D`  | `rgba(255,126,92,0.20)`     |
| 疲惫 | 😴    | 摩卡果冻   | `#B69A7E`  | `#C9B098 → #A08263`           | `#E5D0BC`  | `#2B2319`  | `rgba(182,154,126,0.18)`    |

> **v3.2 配色优化**：软底色（soft）饱和度较 v3.1 提升约 30%，告别莫兰迪扁平感，在浅色背景上对比更清晰。新增"深色软底色"（softDark）字段，深色模式下日期卡片使用深色版本底色（如开心 `#3A2F1A`），避免浅色色块在暗背景上刺眼。

**深色模式（Dark）**：

深色模式下心情色亮度提升约 10%，光晕半径和强度加大，在暗背景上像发光的软糖灯。

| 心情 | 主色       | 渐变（亮→暗）                 | 软底色     | 深色软底色  | 光晕色                     |
| ---- | ---------- | ----------------------------- | ---------- | ---------- | --------------------------- |
| 开心 | `#FFC75B`  | `#FFDA80 → #FFB53A`           | `#FFEAB0`  | `#3A2F1A`  | `rgba(255,199,91,0.28)`     |
| 平静 | `#5EC6F0`  | `#7DD6F8 → #48B5DE`           | `#C5E8F8`  | `#1A2C35`  | `rgba(94,198,240,0.28)`     |
| 低落 | `#AE8DE8`  | `#C4A5F0 → #9670DA`           | `#D5C5F5`  | `#2A2238`  | `rgba(174,141,232,0.28)`    |
| 灵感 | `#EE85CE`  | `#F59EDC → #DD6CBC`           | `#F2C8E8`  | `#321E2B`  | `rgba(238,133,206,0.28)`    |
| 焦虑 | `#FF8E70`  | `#FFA687 → #F57050`           | `#FFCDBF`  | `#34211D`  | `rgba(255,142,112,0.28)`    |
| 疲惫 | `#C5A98E`  | `#D5BCA3 → #B5917A`           | `#E5D0BC`  | `#2B2319`  | `rgba(197,169,142,0.22)`    |

> 深色模式下日期卡片背景使用 softDark 而非 soft，避免浅色色块在暗背景上形成刺眼白块。软底色（soft）仅用于浅色模式和心情选择器悬浮态。

**果冻质感实现要点**：选中态卡片使用 `background: linear-gradient(160deg, 渐变亮, 渐变暗)` + `box-shadow: inset 0 1px 0 rgba(255,255,255,0.5), 0 0 24px 光晕色`，模拟光线从左上照射、果冻顶部高光 + 外围柔光晕染的效果。

#### 4.3 中性色系统（双模式）

浅色模式放弃了原先灰扑扑的 `#4A4A4A`，改用带暖调的深棕灰，让文字也像墨水而非钢筋。深色模式不用冷冰冰的 `#FFFFFF`，而是暖奶油白。

| 用途       | 浅色模式      | 深色模式      | 说明                   |
| ---------- | ------------- | ------------- | ---------------------- |
| 正文文字   | `#2D2620`     | `#F5EDDA`     | 暖墨棕 / 暖奶油白（v3.2 加深/提亮，确保对比度） |
| 次要文字   | `#6B6056`     | `#B5AB9A`     | 辅助信息、备注（v3.2 加深/提亮） |
| 占位文字   | `#948A80`     | `#8A7F75`     | placeholder、引导语    |
| 卡片底色   | `rgba(255,253,249,0.88)` | `rgba(35,31,26,0.82)` | 半透明，v3.2 提高不透明度增强可读性 |
| 导航栏背景 | `rgba(245,242,235,0.85)` | `rgba(28,25,21,0.85)` | 半透明 + 模糊 |
| 分隔线     | `#D5CCC0`     | `#4A443C`     | 暖灰细线（v3.2 加深）  |
| 品牌强调色 | `#FFB938`     | `#FFC75B`     | 蜂蜜金，用于 logo/今日光晕 |
| 页面背景渐变 | `#FBF6EE → #FFF0E0 → #FCE8D8` | `#1A1714 → #221E18 → #1E1A14` | 三色微渐变（见 4.7） |

#### 4.4 字体系统

在原先三族字体基础上补充完整的字号阶梯和行高规范。

**字体族**：

| 用途      | 推荐字体                          | 字重    | 说明                         |
| --------- | --------------------------------- | ------- | ---------------------------- |
| 正文/UI   | `'Inter'` 或 `'Nunito Sans'`      | 400-500 | 清晰现代，阅读舒适           |
| 手写强调  | `'Caveat'` 或 `'Long Cang'`       | 500-600 | 日期标题、心情标签、引导文字 |
| 数字/时长 | `'DM Mono'` 或 `'JetBrains Mono'` | 400     | 复古打印感，方便对齐         |

**字号阶梯**（Type Scale）：

| 令牌       | 字号  | 行高  | 字间距    | 用途                      |
| ---------- | ----- | ----- | --------- | ------------------------- |
| `text-xs`  | 12px  | 18px  | 0.04em    | 时间戳、保存状态、图例    |
| `text-sm`  | 14px  | 22px  | 0.02em    | 次要文本、标签、备注      |
| `text-base`| 16px  | 28px  | 0         | 正文、日记内容            |
| `text-lg`  | 18px  | 26px  | 0         | 卡片标题、学科名          |
| `text-xl`  | 22px  | 32px  | -0.01em   | 分区标题                  |
| `text-2xl` | 28px  | 38px  | -0.02em   | 页面标题、月份标题        |
| `text-3xl` | 36px  | 48px  | -0.02em   | 欢迎页 hero 文字          |
| 手写体     | 可变  | 1.4   | 0         | 日期、引导语（Caveat 18-24px） |
| 等宽体     | 14px  | 1.6   | 0.02em    | 时长标签、统计数字        |

**字体加载方案**：通过 Google Fonts CDN 引入，或本地自托管以优化加载速度。深色模式下正文字重保持 400，不使用细体（≤300），避免亮文字在暗背景上发虚。

#### 4.5 图标系统
- **UI 操作图标**：使用 `lucide-react` 或 `phosphor-react` 轻量线性图标（导航、箭头、设置等），线宽 1.5px，圆角端点
- **心情表达**：使用 Twemoji CDN 保证跨平台统一渲染，备选方案为自定义 SVG 微表情。心情图标在选中态可叠加白色内阴影模拟果冻高光
- **学科标记点**：预设 8 种果冻色小圆点（取心情色板主色 + 额外 2 色），直径 10px，带 `inset 0 1px 0 rgba(255,255,255,0.4)` 高光

#### 4.6 音效（可选增强）

点击心情时播放微小触感音效，增强记录仪式感。作为可选项，默认关闭，用户可在设置中开启。音效风格建议：柔和短促的水滴声或纸页翻动声，避免电子感强的提示音。需遵循浏览器自动播放策略——仅在用户首次交互后触发。

#### 4.7 渐变与光效系统

**页面背景渐变**：

```
/* 浅色模式 */
background: linear-gradient(160deg, #FBF6EE 0%, #FFF0E0 40%, #FCE8D8 100%);
/* 叠加噪点 */
background-image: url('noise.svg'); /* 或 CSS radial-gradient 点状模拟 */
background-blend-mode: overlay;

/* 深色模式 */
background: linear-gradient(160deg, #1A1714 0%, #221E18 40%, #1E1A14 100%);
```

**卡片渐变**（果冻质感核心）：

```
/* 浅色模式 - 普通卡片 */
background: rgba(255, 253, 249, 0.88);
backdrop-filter: blur(12px) saturate(1.2);

/* 浅色模式 - 选中态心情卡片（以开心为例） */
background: linear-gradient(160deg, #FFD66B, #FFA51F);
box-shadow:
  inset 0 1px 0 rgba(255,255,255,0.5),    /* 顶部高光 */
  inset 0 -2px 4px rgba(0,0,0,0.06),       /* 底部微影 */
  0 0 24px rgba(255,185,56,0.20);          /* 外围光晕 */

/* 深色模式 - 选中态心情卡片 */
background: linear-gradient(160deg, #FFDA80, #FFB53A);
box-shadow:
  inset 0 1px 0 rgba(255,255,255,0.15),
  inset 0 -2px 4px rgba(0,0,0,0.2),
  0 0 32px rgba(255,199,91,0.28);
```

**光效层次**（从底到顶）：
1. **页面渐变层**：最底层的暖色微渐变
2. **毛玻璃层**：卡片半透明 + backdrop-filter，让底层渐变隐约透出
3. **内容层**：文字、图标、进度条
4. **内高光层**：`inset 0 1px 0 rgba(255,255,255,...)` 模拟果冻顶部光泽
5. **外光晕层**：`0 0 20-32px rgba(mood,...)` 心情色柔光扩散

#### 4.8 间距与圆角体系

**间距系统**（4px 基准网格）：

| 令牌       | 值    | 用途                           |
| ---------- | ----- | ------------------------------ |
| `space-1`  | 4px   | 图标与文字间距、紧凑内边距     |
| `space-2`  | 8px   | 卡片内小间距、标签间距         |
| `space-3`  | 12px  | 默认卡片内边距、列表项间距     |
| `space-4`  | 16px  | 舒适内边距、分区内部间距       |
| `space-5`  | 24px  | 分区间距、卡片间距             |
| `space-6`  | 32px  | 大分区间距、弹窗内边距         |
| `space-7`  | 48px  | 页面级留白、顶部底部安全间距   |

**圆角系统**：

| 令牌          | 值     | 用途                             |
| ------------- | ------ | -------------------------------- |
| `radius-sm`   | 8px    | 标签、badge、小学科圆点         |
| `radius-md`   | 12px   | 输入框、小按钮                   |
| `radius-lg`   | 16px   | 普通卡片、日期卡片               |
| `radius-xl`   | 20px   | 大卡片、心情选择器、弹窗         |
| `radius-2xl`  | 28px   | 模态框、底部抽屉                 |
| `radius-full` | 999px  | 胶囊按钮、圆形头像、心情图标     |

**设计意图**：圆角比 v2.0 更大更统一，`radius-xl` 以上是"果冻感"的关键——大圆角让元素看起来柔软、可按压。杜绝任何直角。

#### 4.9 阴影系统（分层）

原先只有一个 `0 4px 20px rgba(...)` 万能阴影，现在拆成 4 个层级 + 2 种特效阴影，配合组件层级使用。

**浅色模式**：

| 令牌            | 值                                                           | 用途                 |
| --------------- | ------------------------------------------------------------ | -------------------- |
| `shadow-1`      | `0 1px 2px rgba(155,140,120,0.06), 0 1px 3px rgba(155,140,120,0.04)` | 扁平元素、内嵌标签 |
| `shadow-2`      | `0 2px 8px rgba(155,140,120,0.08), 0 1px 3px rgba(155,140,120,0.05)` | 默认卡片、日期格   |
| `shadow-3`      | `0 8px 24px rgba(155,140,120,0.10), 0 2px 8px rgba(155,140,120,0.06)` | 悬浮卡片、tooltip  |
| `shadow-4`      | `0 16px 48px rgba(155,140,120,0.14), 0 4px 16px rgba(155,140,120,0.08)` | 模态框、底部弹窗   |
| `jelly-inner`   | `inset 0 1px 0 rgba(255,255,255,0.5), inset 0 -2px 4px rgba(0,0,0,0.06)` | 果冻内高光（所有选中态） |
| `mood-glow`     | `0 0 24px {mood.光晕色}`                                     | 心情选中扩散、今日光晕 |

**深色模式**：

| 令牌            | 值                                                           | 用途                 |
| --------------- | ------------------------------------------------------------ | -------------------- |
| `shadow-1`      | `0 1px 2px rgba(0,0,0,0.30), 0 1px 3px rgba(0,0,0,0.20)`     | 扁平元素、内嵌标签 |
| `shadow-2`      | `0 2px 8px rgba(0,0,0,0.35), 0 1px 3px rgba(0,0,0,0.25)`     | 默认卡片、日期格   |
| `shadow-3`      | `0 8px 24px rgba(0,0,0,0.40), 0 2px 8px rgba(0,0,0,0.30)`    | 悬浮卡片、tooltip  |
| `shadow-4`      | `0 16px 48px rgba(0,0,0,0.50), 0 4px 16px rgba(0,0,0,0.35)`  | 模态框、底部弹窗   |
| `jelly-inner`   | `inset 0 1px 0 rgba(255,255,255,0.08), inset 0 -2px 4px rgba(0,0,0,0.15)` | 果冻内高光（暗色） |
| `mood-glow`     | `0 0 32px {mood.光晕色}`                                     | 心情选中扩散（更强） |

**使用规则**：卡片默认 `shadow-2`，悬浮升至 `shadow-3`；弹窗/抽屉固定 `shadow-4`；所有"选中态"元素叠加 `jelly-inner`；心情相关交互叠加 `mood-glow`。阴影使用暖灰 `rgba(155,140,120,...)` 而非纯黑，保持整体暖调。

#### 4.10 深色模式规范

**触发方式**：跟随系统 `prefers-color-scheme: dark`，用户也可手动切换并持久化到 localStorage。

**设计原则**：
- 深色不是纯黑，是**深暖棕**——保留手帐的温度感，不像终端
- 背景使用渐变而非纯色，避免大面积暗色显得压抑
- 卡片使用半透明深色 + 毛玻璃，让背景渐变隐约透出，增加层次
- 心情色光晕在暗色背景上更明显，利用这一点让深色模式更有"夜灯"氛围
- 正文不用纯白 `#FFFFFF`，用暖奶油白 `#F5EDDA`，减少眩光
- 分隔线、边框用暖深色 `#4A443C`，不用纯黑或冷灰

**组件适配要点**：
- 日历日期卡片：深色模式下无记录格虚线改为 `rgba(189,178,168,0.2)`，有记录格使用心情 softDark 色为底色 + `1px solid var(--color-line)` 边框 + `mood-glow`（不再使用浅色 soft，避免白块）
- 日记区信纸横线：浅色模式用 `rgba(200,190,180,0.3)`，深色模式用 `rgba(120,110,100,0.15)`
- 占位文字：深色模式用 `#6B6258`，确保对比度 ≥ 3:1
- 图表（Recharts）：深色模式网格线 `rgba(155,140,120,0.15)`，柱子使用心情渐变色
- 毛玻璃弹窗：深色模式 backdrop-filter 需增加 `brightness(0.8)` 防止过亮

---

### 5. 动效与微交互规范

#### 5.1 动效设计原则
- 所有动效服务于情感反馈，非炫耀技术
- 持续时间控制在 200-400ms，不拖沓
- 使用缓出曲线，让动作感觉自然完成
- 遵循"一个动作一个焦点"原则，避免同时多处动画
- **果冻弹性**：心情选择、卡片添加等"触觉反馈"类动效使用 overshoot 缓动（`cubic-bezier(0.34,1.56,0.64,1)`），让元素像被按了一下弹回来的软糖
- **光晕呼吸**：空状态和今日标记使用光晕脉冲，而非位移动画，营造"在呼吸"而非"在动"的感觉

#### 5.2 具体动效规范

| 场景         | 动画描述                              | 时长   | 缓动函数                         |
| ------------ | ------------------------------------- | ------ | -------------------------------- |
| 页面切换     | 淡入 + 轻微上移 8px                   | 250ms  | `ease-out`                       |
| 心情选中     | 果冻弹性缩放 0.85→1.08→1.0 + 渐变填充 | 220ms  | `cubic-bezier(0.34,1.56,0.64,1)` |
| 心情选中扩散 | 选中后 `mood-glow` 波纹向外扩散消失   | 400ms  | `ease-out`                       |
| 卡片悬浮     | 上移 2px + `shadow-2`→`shadow-3` + `mood-glow` | 200ms | `ease-out`                |
| 学习卡片添加 | 果冻弹入 scale(0.8)→1.05→1.0 + opacity(0→1) | 280ms | `cubic-bezier(0.34,1.56,0.64,1)` |
| 添加弹窗     | 从底部滑入 + 背景毛玻璃渐变 + `shadow-4` | 300ms | `ease-out`                     |
| Tooltip 出现 | 淡入 + 轻微弹性 + `shadow-3`          | 200ms  | `cubic-bezier(0.34,1.56,0.64,1)` |
| 画廊格子悬浮 | 放大 120% + `mood-glow` 亮起          | 200ms  | `cubic-bezier(0.34,1.56,0.64,1)` |
| 时间轴卡片悬浮 | 横移 4px + `shadow-2`→`shadow-3`    | 250ms  | `ease-out`                       |
| 加载翻书动画 | 卡片 3D 翻页 + 翻开闪过心情色微光     | 600ms  | `ease-in-out`                    |
| 空状态呼吸   | 图标 `mood-glow` 脉冲（透明度 0.15→0.30 循环） | 2000ms | `ease-in-out` 循环      |
| 今日光晕脉冲 | 今日卡片 `mood-glow` 透明度微脉冲     | 3000ms | `ease-in-out` 循环               |
| 保存指示     | 绿点亮一下后常驻                      | 300ms  | `ease-out`                       |
| 进度条填充   | 宽度从 0 渐变填充至目标值 + 渐变色流动 | 400ms  | `ease-out`                       |

#### 5.3 动画实现方式
- 优先使用 CSS `transition` 和 `animation`
- 复杂场景（如翻书）使用 CSS `transform` 3D 属性
- 避免 JavaScript 动画以保证性能
- 果冻弹性效果通过 `cubic-bezier(0.34,1.56,0.64,1)` overshoot 缓动实现，无需额外库
- `mood-glow` 脉冲使用 CSS `@keyframes` 控制 `box-shadow` 透明度循环
- 深色/浅色模式切换：颜色变化使用 300ms `ease-out` 过渡，避免突变
- 遵循 `prefers-reduced-motion` 媒体查询，为偏好减少动效的用户提供降级方案（关闭弹性 overshoot，仅保留淡入淡出）

---

### 6. 加载动画设计

#### 6.1 设计理念
纯本地应用加载极快，技术上不需要加载动画。但打开"日记本"的过程本身就是一种仪式感。加载动画在此作为**过渡仪式**而非等待掩盖。

#### 6.2 翻书动画（首次加载）

**场景**：用户首次打开网站、切换月份时

**视觉设计**：
- 画面中央出现圆角矩形卡片（模拟书本封面），奶纸色底，带细腻阴影
- 卡片表面有从左上到右下的隐约折痕线
- 动画序列：
  1. 卡片静止 100ms（让用户感知）
  2. 右侧边缘微微抬起（`rotateY` 初始）
  3. 卡片向右翻开（`rotateY` 过渡），露出内页浅色
  4. 翻开瞬间闪过心情渐变色微光（取上次记录的心情色，若无则蜂蜜金）
  5. 卡片整体淡出，真实内容淡入

**技术参数**：
- 容器：`perspective: 1000px`
- 翻页：`transform: rotateY(-15deg)` → `rotateY(-90deg)`
- 总时长：500-600ms
- 缓动：`ease-in-out`

**卡片上文字**（手写体，淡色）：
> "翻开今天的一页…"

#### 6.3 简化备选：柔光呼吸动画

若翻书动画实现复杂度过高，备选方案：
- 应用 Logo/书本图标居中
- 外围柔光层呼吸式缩放（`scale` 1.0 → 1.1 → 1.0）
- 下方小字"正在打开…"
- 循环播放直至内容就绪，然后淡出
- 总等待时长不超过 1.5s

#### 6.4 触发策略
- **首次加载**：显示翻书/柔光动画
- **月份切换**：可选轻微过渡（月视图卡片整体 300ms 淡入切换），不重复完整翻书
- **页面内导航**：使用页面切换动效（淡入+上移），不触发加载动画
- 绝不每次操作都播放，避免仪式感变成烦人感

---

### 7. 空状态页面设计

#### 7.1 设计理念
空状态不是"什么都没有"，而是"一切正要开始"。每一处空白都应该传递温暖期待感，引导用户踏出第一步。

#### 7.2 场景一：全新用户月视图首页

**视觉呈现**：
- 完整日历网格可见，所有日期格子为虚线边框空白卡片
- 每张空白卡：极浅灰虚线边框，中央无 Emoji，底部时长条位为浅灰虚线
- **今日卡片特殊处理**：
  - 背景微微泛蜂蜜金 `mood-glow`（`0 0 24px rgba(255,185,56,0.20)`）
  - 像被阳光选中，视觉上邀请点击
  - 无文字覆盖，保持日历整洁

**引导文字**（悬浮在日历上方或右侧）：
> "今天想记录什么心情呢？点击今天的卡片开始吧 ✨"
- 手写体 `Caveat`，字号 18px，颜色 `#948A80`
- 带有轻微上下浮动动画（`translateY` ±4px，2s 循环）

**备选方案**：右侧区域展示欢迎卡片
```
┌─────────────────────────────────┐
│  👋 欢迎来到你的学习手帐         │
│                                 │
│  每天记录心情和学习，           │
│  一个月后你会收获一本           │
│  属于自己的时间之书。           │
│                                 │
│        [ 开始记录今天 ]         │
└─────────────────────────────────┘
```
点击按钮直接跳转今日详情页。

#### 7.3 场景二：今日详情页空白

**心情选择器**：
- 全部 6 个选项可见
- 第一个（开心😊）带有微妙脉冲动画（`scale` 1.0→1.05→1.0，2s 循环）
- 像在轻轻招手示意

**学习记录区**：
- 显示虚线占位卡片：
```
┌─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─┐
│  ＋ 添加今天的第一段学习          │
│     比如"React 组件设计"          │
└─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─┘
```
- 点击占位卡片等同于点击 [+ 添加记录] 按钮
- 边框使用心情渐变色（若已选择心情）或默认蜂蜜金 `mood-glow` 虚线

**日记区**：
- 显示浅灰色占位文字（手写体 `Caveat`，颜色 `#948A80` / 深色 `#8A7F75`）：
> "随手写点什么… 今天窗外是什么天气？"
- 点击输入框后占位文字消失

#### 7.4 场景三：回忆页完全空白

**画廊模式**：
- 完整月份矩阵展示，所有格子为虚线空框
- 顶部引导文字：
> "还没有记忆呢。每天记录一点点，这里会慢慢变成你的心情花园 🌱"
- 矩阵下方有一个空状态插画：小花盆里冒出嫩芽

**时间轴模式**：
- 垂直中线显示，两侧完全空白
- 中线底部有一个小萌芽图标 🌱 轻轻摆动
- 引导文字：
> "时间轴上还空空的。从今天开始，让每一天都有迹可循。"

**统计模式**：
- 图表框架可见但无数据
- 柱状图区域显示虚线占位柱体轮廓
- 中央文字：
> "这里会随着时间长出果实 📊"
> "坚持记录一周后，就能看到第一份统计报告"

#### 7.5 空状态设计原则总结

| 场景       | 情感传递       | 引导策略                   |
| ---------- | -------------- | -------------------------- |
| 日历空白   | "今天在等你"   | 今日格微光 + 浮动提示文字  |
| 详情页空白 | "从这里开始"   | 虚线占位卡片 + 手写体引导  |
| 回忆空白   | "时间会填满它" | 花园/萌芽隐喻 + 期待感营造 |
| 统计空白   | "果实需要时间" | 框架可见 + 激励坚持记录    |

---

### 8. 响应式设计概览

#### 8.1 断点策略
- **桌面端**（≥1024px）：左侧导航 + 右侧内容，日历卡片 75px
- **平板端**（768-1023px）：顶部导航 + 全宽内容，日历卡片 55-65px
- **手机端**（<768px）：底部标签导航，日历卡片 40-45px，仅显示 Emoji + 时长条

#### 8.2 移动端适配要点
- 导航从左侧竖栏变为底部横栏（iOS 风格标签）
- 日历卡片缩小，点击进入完整详情
- 画廊矩阵支持横向滚动
- 时间轴卡片全部左对齐，省略交替布局
- 弹窗改为底部抽屉式全宽
- 保持所有动效和空状态设计，不因屏幕变小而牺牲体验

---

### 9. 数据模型与存储

#### 9.1 核心数据结构

```typescript
// 单日记录
interface DayRecord {
  id?: number;                    // IndexedDB 自增主键
  date: string;                   // YYYY-MM-DD 格式，唯一索引
  mood: MoodType;                 // 心情类型
  learnings: LearningItem[];      // 学习记录数组
  diary: string;                  // Markdown 格式日记
  createdAt: number;              // 创建时间戳
  updatedAt: number;              // 最后更新时间戳
}

// 心情类型枚举
type MoodType = 'happy' | 'calm' | 'sad' | 'inspired' | 'anxious' | 'tired';

// 单条学习记录
interface LearningItem {
  id: string;                     // 唯一标识 (nanoid)
  subject: string;                // 学科/项目名
  durationMin: number;            // 学习时长（分钟）
  note: string;                   // 备注
  color: string;                  // 标记颜色（预设色板）
}

// 心情配置（静态数据）
interface MoodConfig {
  type: MoodType;
  label: string;                  // 中文标签
  emoji: string;                  // Emoji 图标
  main: string;                   // 主色值
  light: string;                  // 渐变亮色
  dark: string;                   // 渐变暗色
  soft: string;                   // 浅色模式软底色
  softDark: string;               // 深色模式软底色（v3.2 新增）
  glow: string;                   // 光晕色
  gradient: string;               // 完整渐变值
}
```

#### 9.2 存储方案
- **数据库**：IndexedDB，通过 `Dexie.js` 封装操作
- **数据库名**：`StudyJournalDB`
- **表结构**：`records` 表，以 `date` 字段为唯一索引
- **读写策略**：日记区输入防抖 1.5s 后自动保存；学习记录和心情选择即时保存
- **数据导出**：JSON 格式一键导出，包含所有 DayRecord 数据
- **数据导入**：选择 JSON 文件合并或覆盖导入

#### 9.3 初始数据
- 应用首次启动无预置数据
- 心情配置表为静态常量，不存入数据库
- 首次打开展示空状态欢迎页

---

### 10. 技术栈选型

#### 10.1 前端框架与构建

| 项目     | 选型            | 说明                   |
| -------- | --------------- | ---------------------- |
| 框架     | React 18+       | 生态丰富，适合单页应用 |
| 构建工具 | Vite            | 极速开发体验，开箱即用 |
| 语言     | TypeScript      | 类型安全，提升代码质量 |
| 路由     | React Router v6 | 页面导航管理           |

#### 10.2 样式与 UI

| 项目       | 选型                            | 说明                 |
| ---------- | ------------------------------- | -------------------- |
| CSS 框架   | Tailwind CSS                    | 原子化样式，快速构建 |
| 自定义样式 | CSS Modules / Styled Components | 复杂动画和特殊效果   |
| 图标库     | Lucide React                    | 轻量线性图标         |
| Emoji      | Twemoji CDN                     | 跨平台统一心情表情   |

#### 10.3 数据与状态

| 项目       | 选型                       | 说明                        |
| ---------- | -------------------------- | --------------------------- |
| 本地数据库 | Dexie.js                   | IndexedDB 封装，Promise API |
| 状态管理   | React Context + useReducer | 轻量，够用不杀鸡用牛刀      |
| 表单处理   | React Hook Form            | 学习记录添加表单            |
| ID 生成    | nanoid                     | 学习记录项唯一标识          |

#### 10.4 可视化与特效

| 项目     | 选型           | 说明                   |
| -------- | -------------- | ---------------------- |
| 图表库   | Recharts       | React 原生，轻量，够用 |
| 动画增强 | Framer Motion  | 复杂动效（按需引入）   |
| Markdown | react-markdown | 日记区 Markdown 渲染   |

#### 10.5 部署与扩展

| 项目       | 选型             | 说明                     |
| ---------- | ---------------- | ------------------------ |
| 部署平台   | Vercel / Netlify | 免费静态托管，自动 CI/CD |
| 自定义域名 | 可选             | 视需要配置               |
| PWA 支持   | 可选扩展         | 可安装到桌面，离线缓存   |

---

### 11. 开发路线图

#### 11.1 阶段划分

**P0 - 核心 MVP（最小可用产品）** ✅ 已完成
- [x] 项目脚手架搭建（React + Vite + Tailwind + Router）
- [x] IndexedDB 数据层（Dexie.js 初始化 + CRUD 封装）
- [x] 月视图日历组件（日历网格 + 日期卡片 + 月份切换）
- [x] 今日详情页（心情选择器 + 学习记录添加/删除 + 日记输入）
- [x] 页面路由与导航框架
- [x] 基础样式主题配置（颜色变量、字体引入）

**P1 - 完整体验**
- [ ] 回忆页（心情画廊矩阵 + 时间轴视图）
- [ ] 统计页（学习时长柱状图 + 学科环形图）
- [ ] 加载动画（翻书动画）
- [ ] 空状态页面设计实现
- [ ] 微交互动效（心情选中弹性、卡片悬浮、添加动画）
- [ ] 数据导出/导入功能
- [ ] 响应式布局适配

**P2 - 增强与优化**
- [ ] PWA 支持（可安装到桌面，离线缓存）
- [x] 主题切换（浅色/深色/跟随系统）✅ 已在 P0 实现
- [ ] 自定义心情（允许用户添加/编辑心情类型）
- [ ] 数据云同步（可选后端服务）
- [ ] 日记搜索功能
- [ ] 年度回顾报告（年度学习统计 + 心情曲线）

---

### 12. 附录

#### 12.1 设计灵感参考
- **色彩灵感**：水果软糖/果冻质感、日式和菓子半透明层次、暖色微渐变
- **交互灵感**：iOS 健康 App 的情绪记录、Day One 日记的时间轴、GitHub 贡献图、Bear App 的温暖排版
- **氛围参考**：MUJI 无印良品的简约自然 + 治愈系 App（如周读、Notion 植物壁纸）的柔彩质感
- **深色模式参考**：Arc Browser 暖色暗模式、Telegram 暗色暖调

#### 12.2 文档版本演进
- **v1.0**（项目名称暂定）：初稿，确定项目定位、核心功能、低保真布局、视觉方向、技术选型、数据模型草稿
- **v1.1**：补充中保真视觉设计（情绪氛围、莫兰迪心情色板、字体系统、组件形态、动效规范、响应式概览）
- **v2.0**：完整整合版，新增加载动画设计、空状态页面设计、完整数据模型与存储方案、开发路线图
- **v3.0 终稿**：在 v2.0 基础上查漏补缺，补回完整 ASCII 低保真线框图（月历、详情页、回忆三视图、添加记录弹窗）、图例说明、音效可选增强、PWA 技术栈说明等遗漏细节
- **v3.1 样式美化版**：从设计师视角全面重构视觉体系——莫兰迪扁平色升级为"柔彩果冻治愈风"，新增渐变与光效系统、间距与圆角体系、分层阴影系统、完整深色模式规范、字号阶梯；第 3 节组件样式同步更新引用新设计令牌；第 5 节补充果冻弹性动画与光晕呼吸动效
- **v3.2 配色优化版**（本文档）：P0 开发后配色修复——soft 色饱和度提升 30%（告别莫兰迪扁平感），新增 softDark 字段用于深色模式日期卡片底色；中性色全面调整（浅色文字加深为 `#2D2620`，深色文字提亮为 `#F5EDDA`），卡片不透明度从 0.72/0.68 提至 0.88/0.82，分隔线加深；今日详情页顶部渐变改为主题感知方案（浅色 `hexToRgba(soft, 0.35)` 水彩洇开 / 深色 `hexToRgba(main, 0.08)` 夜灯微光），修复深色模式下心情色块遮文字问题；DateCard 和 MoodSelector 悬浮态均适配 softDark

---

**文档版本**：v3.2 配色优化版  
**最后更新**：2026年7月9日  
**文档状态**：开发设计定稿，进入开发阶段
