# 🌸 学习手帐 · 花期 (Blossom) — 开发设计文档 v5.0

> **每一天，开一朵花。**
> 本文档取代 v4.0/v4.1《纸上手帐 · 编辑风》，是当前唯一有效的设计规范。
> 高保真效果示意图见 `design/mockup.html`（浏览器打开）与 `design/shots/*.png`。
> 实施状态以 **§10 实施状态总表** 为准（✅ 已实现 / 🚧 进行中 / ⬜ 未实现）。

---

### 目录
0. 本版导读：为什么再重构（v4.x 问题诊断与花期主张）
1. 项目概述
2. 核心功能模块
3. 视觉设计系统（前端核心）
4. 签名元素体系（花朵生成器 / 花环 / 新芽徽章 / 花束）
5. 页面设计
6. 动效与微交互规范
7. 响应式设计
8. 数据模型与存储
9. 技术架构与实现约定
10. **实施状态总表**
11. 附录

---

## 0. 本版导读：为什么再重构

### 0.1 v4.x「纸上手帐 · 编辑风」问题诊断

v4.0/v4.1 解决了 v3.x「素、糊、平」的对比度问题，但落地后仍「没有达到预期」，根因是**设计方向**而非执行：

| # | 问题 | 根因 |
|---|------|------|
| 1 | **好看，但「与我无关」** | 印章、胶带、缝线是对「物件」的模拟，用户记录后得不到情感回馈，缺回访动机 |
| 2 | **回顾没有奖励感** | 月历/画廊只是数据的格子陈列，坚持记录一个月和记录三天，视觉上差别不大 |
| 3 | **配色挑人** | 暖纸黄底易显旧；v4.1 用三套配色主题打补丁，反而稀释了品牌识别 |
| 4 | **隐喻不生长** | 「盖章」是完成态隐喻，盖完就结束了；没有随时间累积、生长的叙事 |
| 5 | **签名元素是负担** | 和纸胶带/缝线/打孔等装饰实现成本高、移动端适配差，且「一屏至多一个」的规则说明它容易被滥用 |

### 0.2 v5.0 设计主张：花期 · 生长手帐

从「模拟一本纸手帐」转向「**养一座花园**」。核心闭环：

```
记录心情 → 开一朵花（花色=心情，茎高=学习时长）
        → 月视图的花田随之生长
        → 月末花田扎成一束「月度花束」海报（可保存/分享）
        → 想看到下一片繁茂的花田 → 明天继续记录
```

**五条核心主张**：

1. **记录即生长**：每一个记录行为都转化为花园里可见的生命——花会开、茎会长、田会满。坚持与懈怠在视觉上立刻可辨。
2. **一套签名配色**：不再做多配色主题。「晨雾白 × 深叶绿 × 杏橙一点」就是「花期」本身；深色模式是「夜色花房」。识别度优先于可定制性。
3. **淡底浓点（沿用）**：大面积晨雾白与柔色浅底，高饱和只出现在花朵、今日花环、主按钮上。一屏一个视觉主角。
4. **排版有中西节奏（沿用并强化）**：中文衬线大标题 + Fraunces 斜体拉丁小注（"July, in bloom."）+ 等宽数字，是页面的签名组合。
5. **装饰即功能**：花朵不是贴图装饰，它就是数据本身（花色=心情、茎高=时长、空圆环=未记录）。花园里没有任何无信息量的元素。

**不变的部分**：产品定位、六大功能模块（含自定义心情）、数据模型、本地存储策略、技术栈、深浅双模式。v5.0 是视觉语言、交互叙事与留存机制的升级，数据层零改动。

**移除的部分**：三配色主题切换（coral/teal/violet）、和纸胶带、缝线分隔、题字竖线、心情印章 SVG 图标体系（由花朵生成器取代）。

---

## 1. 项目概述

### 1.1 项目名称
**花期 (Blossom) · 学习手帐** — 一个注重 UI 美感、生长式回顾体验的个人学习日记与心情追踪 Web 应用。

### 1.2 核心定位
**会生长的数字手帐**：记录每天学什么、心情如何、有什么想法。每一天开一朵花，每个月长成一片花田，月末扎成一束花。回顾过去，像走进一座自己种出来的花园。

### 1.3 核心痛点与解决方案
- **痛点**：记录类应用坚持不下来——记录没有即时回馈，回顾没有累积奖励。
- **解决**：把「记录」翻译成「生长」。心情是花色、时长是茎高、坚持是花田的密度、月末的花束是坚持的证明。

### 1.4 关键设计原则
- 不做计时器/番茄钟，不与现有工具重叠
- 手动填写学习内容与时长，关注记录与回顾
- UI 设计优先：每一屏都要「养眼」——低饱和底、柔色花、充足留白
- 留存靠生长叙事：新芽徽章（连续天数）、花田密度、月度花束
- 便捷访问：Web 应用，打开即用，本地存储，零数据上传，绝对隐私
- 仪式感：每天的第一朵花、每月的最后一束花

---

## 2. 核心功能模块

### 2.1 每日心情记录（盖花）
- 选择当天心情（内置 6 种：开心、平静、低落、灵感、焦虑、疲惫；支持自定义心情）
- 每种心情对应一种**花**（花名 + 花色组 solid / ink / tint 三档）
- 选中即「盖下一朵花」：今日页的大花实时变为该心情的花色，bloom 动效落下
- 自定义心情沿用 emoji + 自选色，花朵以通用花型渲染（花瓣取自定义色）

### 2.2 学习记录（养花）
- 添加多条学习记录（学科/项目名称、时长小时+分钟、简短备注、色点）
- **园丁清单式**展示：色点 + 学科 + 备注 + 等宽时长，行间 hairline
- 支持点击编辑、删除
- 底部汇总当日总学习时长；**当日总时长映射今日花朵的茎高与花径**

### 2.3 日记/想法记录
- 信纸式自由书写区，支持简单 Markdown（加粗、列表、标题、引用）
- 无标题栏，衬线正文，鼓励随手记录
- 自动保存至本地（防抖 1.5s），右下角松绿圆点 + 「已保存 HH:mm」

### 2.4 花田月视图（首页）
- 整月花田网格：每格 = 日期数字 + 一朵花（花色=心情、茎高=当日总学习时长 0~6h 映射）
- 未记录的日子：1px 虚线空圆环（「待种下」）；未来的日子：淡化的空环
- 今日：杏橙手绘感花环 + 浅蜜桃底，是全页第一视觉焦点
- 悬浮 tooltip：日期 + 心情花名 + 时长 + 日记摘录
- 底部图例：六色小花 + 花名；月度小结「本月 N 朵 · 共 Xh Ym」

### 2.5 回忆与统计
- **时光轴**：按时间流展示过往记录（心情花 + 学科胶囊 + 日记摘录）
- **学习统计**：每日时长柱状图（柱色=当日心情花色）、学科分布环形图、三枚摘要卡
- **月度花束海报**（v5.0 新增，招牌留存功能）：月末将本月开过的花扎成一束竖版海报，标注「N 天 · Xh Ym · YYYY.MM」，可保存为图片分享
- **新芽徽章**（v5.0 新增）：连续记录天数，出现在今日页页眉，鼓励不断档

### 2.6 本地数据管理
- 所有数据存储于浏览器 IndexedDB，离线可用，无后端
- JSON 导出/导入备份
- 云同步为 P4 候选（沿用既有结论）

---

## 3. 视觉设计系统（前端核心）

### 3.1 设计理念

**风格命名**：花期 · 生长手帐（Blossom Garden Journal）。

**关键词**：晨雾、花园、生长、柔色、留白、中西排版节奏

**视觉哲学**：
- **像清晨的花园，不像温室塑料棚**：底色是带一丝绿意的晨雾白（刻意避开 v4.x 发黄的暖纸），花朵是唯一的高饱和元素
- **花是数据，不是贴纸**：花朵只在承载信息时出现（心情、时长、统计），页面不靠花朵插图装饰
- **柔而不糊**：白色实卡 + 1px 轮廓线 + 柔和远影，层级靠明度差表达（沿用 v4 表面体系，阴影更轻更远）
- **空即是「待种下」**：空状态全部表达为「虚线空圆环」——没有记录的日子不是缺失，是还没种下的位置

### 3.2 色彩系统

#### 3.2.1 中性色与品牌色（双模式，单一主题）

| 令牌 | 浅色模式（晨园） | 深色模式（夜色花房） | 用途 |
|------|------------------|----------------------|------|
| `paper` | `#F5F7F0` | `#131815` | L0 页面底（微渐变 + 光斑 + 噪点，见 3.2.4） |
| `card` | `#FFFFFF` | `#1B211D` | L1 卡片面（实色，不透明） |
| `overlay` | `#FFFFFF` | `#232A24` | L2 浮动层（tooltip/弹窗/抽屉） |
| `ink` | `#2C322A` | `#E9EDE3` | 正文墨色（带绿调的黑/米白） |
| `ink-soft` | `#6D7466` | `#9AA594` | 次要文字、备注 |
| `ink-faint` | `#A7AC9E` | `#6B7666` | 占位文字、图例、时间戳 |
| `hairline` | `#EBEEE0` | `#263029` | 内部格线、信纸横线 |
| `keyline` | `#E2E6D4` | `#323D34` | 卡片外轮廓线（1px） |
| `brand` | `#3E7C59` | `#6FB98A` | **深叶绿**：主按钮、选中态、导航激活 |
| `brand-ink` | `#2F5C42` | `#8FD0A6` | 叶绿 hover/按下态 |
| `pine` | `#57A773` | `#6FB98A` | 亮绿：保存成功、花茎/叶片、正向状态 |
| `accent` | `#E8845C` | `#F09A72` | **杏橙**：今日花环、新芽徽章、「今天」按钮（唯一暖色重点） |
| `accent-ink` | `#C9663F` | `#F2AF8C` | 杏橙 hover/按下态 |

> **色彩纪律**：`accent` 杏橙一屏至多出现一处，永远留给「今天/当前连续」；`brand` 叶绿承担操作与选中；花朵色不承担 UI 功能色职责（链接、按钮禁用花色）。

#### 3.2.2 心情花色板（花园六色）

每种心情 = 一种花 + 三档色：**solid**（花瓣实色/选中态/图表）、**ink**（深调，浅底上的文字）、**tint**（浅底，大面积铺垫、选择器底色）。

| 心情 | 花名 | solid | ink | tint |
|------|------|-------|-----|------|
| 开心 | 向日葵金 | `#F0B429` | `#8A6410` | `#FBF1D6` |
| 平静 | 绣球蓝 | `#6C9BD1` | `#33597F` | `#E3EDF7` |
| 低落 | 薰衣草紫 | `#9B8FC9` | `#584E85` | `#ECE8F5` |
| 灵感 | 樱粉 | `#E58AAE` | `#8F3E61` | `#FAE6EF` |
| 焦虑 | 虞美人橙 | `#E98A5F` | `#8F4B28` | `#FBE8DE` |
| 疲惫 | 鼠尾草绿 | `#93A693` | `#556352` | `#E8ECE4` |

深色模式（solid 提亮、ink 转浅、tint 转深底）：

| 心情 | solid | ink | tint |
|------|-------|-----|------|
| 开心 | `#F5C35A` | `#F0DCA8` | `#3A3016` |
| 平静 | `#8FB6DE` | `#CFE2F2` | `#1E2A38` |
| 低落 | `#B3A8D9` | `#DCD6F0` | `#262238` |
| 灵感 | `#EEA3C0` | `#F6D3E2` | `#38202C` |
| 焦虑 | `#F0A077` | `#F6DAC8` | `#382518` |
| 疲惫 | `#A9BCA9` | `#D8E2D4` | `#242C22` |

**学科标记点色板**（8 色）：`#F0B429` `#6C9BD1` `#9B8FC9` `#E58AAE` `#E98A5F` `#93A693` `#57A773` `#4A7BA6`

#### 3.2.3 色彩使用法则
- 一页之中 solid 花色面积 ≤ 12%（花朵本体 + 图表）
- tint 上文字必须用同族 ink；禁用 solid 当正文文字色
- 空圆环、虚线、图例等「未发生」元素一律 `ink-faint` / `hairline`，不用灰黑色
- 禁止：大面积彩色渐变卡片、彩色外发光、超过两种花色出现在同一小组件内（花束海报除外——它就是颜色的庆典）

#### 3.2.4 页面底（晨雾）
```css
/* 浅色模式 */
background: linear-gradient(170deg, #F8FAF3 0%, #F5F7F0 45%, #F0F3E8 100%);
/* 叠加两团大面积虚化光斑（radial-gradient，opacity ≈ 0.5）：
   左上薄荷 #DFF0E2、右下蜜桃 #FCE8DC —— 晨光与花影 */
/* 深色模式 */
background: linear-gradient(170deg, #161B17 0%, #131815 45%, #10130F 100%);
/* 光斑改为极暗的绿 #1C2B20 与褐 #2B2118 */
```
再叠 SVG 噪点（tile 200×200）：浅色 `opacity: 0.04`，深色 `opacity: 0.05`。

### 3.3 表面与层级体系

沿用三级表面，阴影更轻、半径更大（花园的空气感）：

| 层级 | 构成 | 用于 |
|------|------|------|
| L0 纸底 | 微渐变 + 光斑 + 噪点 | 页面背景 |
| L1 卡片 | `card` 实色 + `1px keyline` + `shadow-1`，圆角 20px | 所有内容卡 |
| L2 浮动层 | `overlay` 实色 + `backdrop-filter: blur(8px)` + `shadow-3/4` | tooltip、弹窗、底部抽屉、导航顶栏 |

**阴影**（阴影色带绿调 `rgba(44,50,42,…)`，深色用纯黑系）：

| 令牌 | 浅色模式 | 用途 |
|------|----------|------|
| `shadow-1` | `0 1px 2px rgba(44,50,42,0.04), 0 8px 24px rgba(44,50,42,0.05)` | L1 默认 |
| `shadow-2` | `0 2px 6px rgba(44,50,42,0.06), 0 12px 32px rgba(44,50,42,0.08)` | 卡片悬浮 |
| `shadow-3` | `0 2px 8px rgba(44,50,42,0.08), 0 16px 40px rgba(44,50,42,0.10)` | tooltip、浮层 |
| `shadow-4` | `0 4px 12px rgba(44,50,42,0.08), 0 24px 56px rgba(44,50,42,0.14)` | 弹窗、抽屉 |

深色模式：近影 `rgba(0,0,0,0.35)`、远影 `rgba(0,0,0,0.28)`，半径不变。

### 3.4 字体与排版系统

| 角色 | 字体栈 | 用途 |
|------|--------|------|
| 展示衬线 | `'Noto Serif SC', 'Songti SC', 'STSong', serif` | 页面大标题、日期、分区标题、日记正文 |
| 拉丁点缀 | `'Fraunces', serif`（italic） | 英文小注："in bloom." / "July's field" |
| UI 正文 | `system-ui, 'PingFang SC', 'Microsoft YaHei', sans-serif` | 按钮、标签、表单、清单 |
| 等宽数字 | `'IBM Plex Mono', monospace` | 时长、日期数字、统计值 |

**字号阶梯**：

| 令牌 | 字号/行高 | 字重/字族 | 用途 |
|------|-----------|-----------|------|
| `display` | 40px / 48px | serif 600 | 月份大标题「七月的花田」 |
| `h1` | 30px / 38px | serif 600 | 页面标题（今日日期） |
| `h2` | 19px / 28px | serif 600 | 卡片标题（今日学习、今日想法） |
| `title` | 16px / 24px | sans 600 | 学科名、清单主文字 |
| `body` | 15px / 26px | sans 400 | 表单、正文 |
| `diary` | 17px / 32px | serif 400 | 日记正文 |
| `small` | 13px / 20px | sans 400 | 备注、辅助信息 |
| `caption` | 12px / 16px | sans 500, +0.03em | 图例、时间戳、保存状态 |
| `num` | 13~15px | mono 500 | 时长、日期数字 |
| `num-lg` | 28px / 36px | mono 500 | 统计大数字、合计时长 |

**排版规则**：
- 页面标题组 = 衬线大标题 + caption 信息行 + Fraunces 斜体拉丁小注（一中一西，签名组合）
- 每日一句：2px 叶绿左边框 + 衬线引文 + caption「每日一句」
- 数字一律等宽；时长格式 `2h 30m`
- 卡片标题不用竖线装饰（题字竖线已移除），靠衬线与留白建立层级

### 3.5 图标系统
- UI 图标：`lucide-react`，`stroke-width: 1.75px`，尺寸 18/20px，着色 `ink-soft`，悬浮转 `ink`
- **心情不再使用 emoji 与线性印章图标**：一律由花朵生成器渲染（见 §4.1）
- emoji 仅存留于自定义心情（用户自选表达）与空状态辅助文案
- 新芽徽章、分享、添加等签名小图标见 §4

### 3.6 间距、圆角与网格
- **间距**（8pt）：`4 / 8 / 12 / 16 / 24 / 32 / 48 / 64`；卡片 padding ≥ 28px，区块间距 ≥ 24px
- **圆角**：`r-sm 6 / r-md 10 / r-lg 14 / r-xl 20 / r-2xl 28 / r-full 999`；L1 卡片一律 `r-xl 20px`（比 v4 更柔和，呼应花形曲线）
- **布局网格**：桌面端左侧固定导航 220px + 内容区；内容限宽：今日页 860px、花田 1040px、时光 720px、统计 880px

---

## 4. 签名元素体系

花期的「物性」来自以下四个签名元素。它们全部**承载数据**，不是装饰。

### 4.1 花朵生成器（Flower）— 核心组件

统一由 `<Flower />` React 组件程序化生成 SVG 花朵，全应用唯一花卉来源。

**构成**（viewBox 内自适应）：
- **花瓣**：绕花心旋转的圆润水滴形 path，5~8 瓣；双层结构——外层大花瓣 `solid` 38% 透明（深色模式改用 tint 提亮），内层小花瓣 `solid` 实色，制造层次
- **花心**：白色或 `card` 色小圆
- **花茎**（可选）：微弯二次贝塞尔曲线 + 1~2 片小叶，`pine` 绿色；茎高 `stemH` 映射学习时长（0~6h → 归一化）
- **风格**：扁平、圆润、克制，无渐变堆砌

**Props**：`{ mood?: MoodConfig | null, color?: string, size: number, stem?: number /* 0~1 */, petals?: number, variant?: 'full' | 'head' }`
- `mood` 与 `color` 二选一：内置心情传 mood（取三档色），学科色/自定义场景传 color
- `stem=0` 时不画茎（选择器、图例用 `variant="head"` 仅花头）
- 自定义心情：通用 6 瓣花型，花瓣取其 solid 色

**尺寸约定**：今日页大花 120~160px、心情选择器 28px（花头）、花田格 36~52px（含茎）、时光轴 24px、统计摘要 28px、花束 40~56px。

### 4.2 今日花环（Today Ring）
- 花田中今日格外一枚**杏橙手绘感圆环**：直径约为格宽的 62%，1.5px `accent`，非正圆（SVG 路径带轻微抖动），旋转 -4deg
- 今日格底色：蜜桃 `#FCE8DC` 40%（深色： `#2B2118`）
- 今日未记录时，花环内放一枚 `accent` 虚线空圆——「等你种下今天」

### 4.3 新芽徽章（Streak Sprout）
- 位置：今日页页眉右侧（移动端同行右端）
- 构成：浅绿底（`pine` 12%）胶囊 + 双叶新芽 SVG（`pine`）+ 等宽数字「连续 N 天」
- N=0 时不显示；N≥1 显示；断档后从 1 重新生长
- 连续天数计算：从今天（或昨天，若今天未记录）向前逐日回溯有任意记录（心情/学习/日记）的连续天数

### 4.4 月度花束海报（Bouquet Poster）
- 位置：统计页底部；每月最后一天起在今日页也出现入口
- 构成（竖版 320×~460px）：晨雾/浅绿渐变底 + 衬线标题「M月花束」+ Fraunces 小注 "a bouquet of {Month}" + **花束 SVG**（本月出现过的花色 5~9 朵，茎向底部收拢，系一枚杏橙蝴蝶结）+ 等宽一行「N 天 · Xh Ym · YYYY.MM」+「分享本月花束」主按钮
- 花朵选择：按本月各心情出现频次取前 6 种，频次越高花朵越大
- 分享：将花束 SVG 序列化绘制到 canvas，导出 PNG 下载（无第三方依赖）
- 空月：虚线空环 + 「这个月还没有开花，种下第一朵吧」

### 4.5 空状态统一语言：虚线空圆环
所有「没有数据」的表达都是 `1px dashed ink-faint` 圆环 + 一句衬线引导语：
- 花田空格：空环（无文案，图例说明语义）
- 今日未选心情：选择器全部未选态 + 「盖下一朵花，记录今天」
- 时光空白：「还没有记忆。每天种一朵花，这里会长成你的小径」
- 统计空白：「坚持记录一周，这里会长出第一份报告」

---

## 5. 页面设计

### 5.1 整体布局

桌面端：**左侧固定导航（220px 白卡）+ 右侧内容区**。

```
┌────────────┐  ┌───────────────────────────────────────────┐
│ ✿ 花期      │  │  页眉：衬线大标题 + caption + Fraunces 注 │
│  Blossom   │  │                        [🌱 连续 12 天]    │
│            │  ├───────────────────────────────────────────┤
│ ❀ 今日     │  │                                           │
│ ▦ 花田     │  │         主内容区（按页型限宽）             │
│ ◷ 时光     │  │                                           │
│ ▤ 统计     │  │   今日 860 / 花田 1040 / 时光 720         │
│            │  │                                           │
│ ⚙ 设置     │  │                                           │
│            │  │  v5.0 · 本地存储中                        │
└────────────┘  └───────────────────────────────────────────┘
```

- Logo 区：一朵小花（开心金）+ 衬线「花期」+ Fraunces "Blossom"
- 导航项：今日 `/day/{today}`、花田 `/`、时光 `/memory`、统计 `/stats`、设置 `/settings`；搜索入口放设置页或导航次要位
- 激活态：左侧 3px 叶绿竖条 + 浅绿底块（`pine` 10%）+ `ink` 文字
- 移动端：底部 tab bar（今日/花田/时光/我的），激活项叶绿

### 5.2 今日页 — 盖花（核心屏，860px）

**页眉**：衬线 `h1`「7月26日」+ caption「星期六 · Sat」+ Fraunces "in bloom."；右侧新芽徽章。
**每日一句**：细叶绿左边框引言卡（衬线，如「把时间种下去，就会开花。」），quotes 轮换。

**今日花卡（hero，本屏主角）**：
- 左侧：今日大花（120~160px，带茎）。未记录心情：通用花型 `hairline` 虚线花瓣 + 「待种下」
- 右侧：衬线「今天是什么心情？」+ 六枚心情选择器（每枚 = `tint` 圆底 56px + 花头 28px + 下方花名 caption）：
  - 未选：`tint` 底 + `solid` 花瓣
  - hover：花头放大 1.08
  - 选中：`solid` 实底 + 白色花瓣 + bloom-in 动效（240ms，轻微回弹，像花开）
- 底部 caption：「盖下一朵花，记录今天」
- 选中后大花同步变色 + bloom-in；当日总学习时长驱动大花茎高/花径缓慢生长

**今日学习卡（园丁清单）**：
- 标题行：衬线 `h2`「今日学习」+ 右侧「+ 添加」keyline 描边胶囊
- 清单行 = 10px 学科色圆点 + 学科名（`title`）+ 备注（`small` ink-soft）+ 右端等宽时长（`num`）；行间 1px hairline；hover 行底 `paper`；点击编辑、行尾 ✕ 删除
- 末行合计：`caption`「合计」+ `num-lg`「3h 45m」
- 空行：虚线占位行「＋ 记下今天的第一段学习」

**今日想法卡（信纸）**：
- `h2`「今日想法」
- 信纸：`card` 面 + keyline + r-xl，内部 32px 横线（hairline），正文 `diary` 衬线 17px
- 占位（ink-faint）：「随手写点什么… 今天窗外是什么天气？」
- 右下保存指示：松绿圆点 + caption「已保存 14:32」

### 5.3 花田月视图（首页，1040px）— 招牌屏

- 页眉：衬线 `display`「七月的花田」+ caption「2026 · 第 30 周」+ Fraunces "July's field"；右侧 ◀ ▶ 圆形 keyline 按钮 +「今天」杏橙描边胶囊
- 星期行：caption「一 二 三 四 五 六 日」（周一起始）
- 网格：白卡大容器（r-xl + keyline + shadow-1），7 列，格间距 0，格间 1px hairline
- **单元格**（桌面 min-height 96px，padding 8px）：
  - 左上：日期数字 `num` 12px ink-soft；非本月 ink-faint + `paper` 底
  - 中央：花朵（36~52px，花色=当日心情，茎高=当日总时长 0~6h 归一化，最短也有小苗）
  - 未记录：1px dashed 空圆环（ink-faint）；未来日：空环 50% 透明
  - 今日：杏橙花环 + 蜜桃底（§4.2）
  - hover：整格 `paper` 底渐入 + 花朵放大 1.08 + tooltip
- **Tooltip**（L2，深色反色卡）：日期 · 星期 + 心情花名标签（tint 底 ink 字）+ `num` 时长 + 日记前两行（`small`）
- 底部 footer：左侧图例（六色花头 + 花名横排）；右侧 caption「本月 18 朵 · 共 32h 15m」

### 5.4 时光（回忆时间轴，720px）

- 页眉「时光」+ Fraunces "pressed flowers, kept days."
- 垂直时间轴：左列等宽日期（`num` 7/25 + caption 星期，宽 56px）；中间 1px hairline 纵线，节点 8px 心情 solid 圆点；右列记录卡：
  - 卡内首行：花头 24px + 心情花名标签（tint 底 ink 字胶囊）+ 右侧学科胶囊 ×N（色点 + 学科 + 时长）
  - 日记摘录 2~3 行（`small` ink-soft，衬线）
- 卡片进入视口 fade-up；无限滚动（沿用现有分页）

### 5.5 统计页（880px）

- 页眉「统计」+ Fraunces "the month, counted."
- **三枚摘要卡**：总学习天数（`num-lg` + caption 天）/ 总时长 / 最常心情（花头 28px + 花名）
- **每日学习时长柱状图**：近 14 天圆角柱（顶圆角 6px），柱色=当日心情 solid（无记录：hairline 小刻度线），网格线 1px dashed hairline，坐标 caption ink-faint
- **学科分布环形图**：SVG donut，分段学科色板实色，中心 `num-lg` 总时长 + caption「本月」；右侧图例行（色点 + 学科 + 等宽时长）
- **月度花束海报**（§4.4）：左海报 + 右侧说明文案（衬线 h2「月末，把花田扎成一束」+ 正文说明 + 本月心情频次标签组）

### 5.6 搜索 / 设置 / 年度回顾
- **搜索**：沿用现有功能；卡片与文字 token 跟随新体系；结果条目带心情花头
- **设置**：分组卡片（外观：深/浅色切换；**配色主题选项移除**；数据：导入/导出；关于）；自定义心情管理沿用（MoodEditModal 视觉对齐）
- **年度回顾**：沿用现有数据逻辑；格子改「花田密度」视觉（有记录格=花色圆点，淡化矩形方块感），标题与文案对齐花期语气

### 5.7 弹窗与抽屉
- 添加学习记录：桌面居中弹窗（L2 overlay，r-2xl，shadow-4，宽 400px）；移动端底部抽屉
- 入场：fade + translateY(12px)→0，220ms ease-out；背景 `ink` 20% 遮罩
- 表单：标签 `small` ink-soft；输入框 `card` 底 + keyline，聚焦 keyline 变 `brand` + 2px brand 15% 晕环
- 学科色点选择：8 色 16px 圆点横排，选中态外圈 2px 同色描边
- 主按钮：叶绿实底白字 r-md；次按钮：keyline 描边

### 5.8 加载仪式
- 翻书动画替换为**花开动画**：晨雾底中央一枚花苞（闭合花瓣）→ 花瓣展开 bloom（600ms）→ 淡出进入应用
- 文案：衬线「等一朵花开…」；仅每个会话首次进入播放（沿用 sessionStorage 机制）

---

## 6. 动效与微交互规范

### 6.1 原则
- 模拟**植物生长的物理感**：花开轻弹、小苗微晃、长茎渐长——不做果冻弹跳、不做波纹
- 时长 150~300ms；花开允许一次轻微 overshoot（生命力），其余缓出
- 标准缓动 `cubic-bezier(0.2, 0.9, 0.3, 1)`；花开缓动 `cubic-bezier(0.34, 1.4, 0.64, 1)`
- 遵循 `prefers-reduced-motion`：降级为纯淡入淡出

### 6.2 动效清单

| 场景 | 动画 | 时长 | 缓动 |
|------|------|------|------|
| 盖花（心情选中） | scale 0.6→1 + rotate -8°→0 + opacity 0→1（bloom-in） | 240ms | 花开缓动 |
| 今日大花变色 | 花瓣颜色过渡 + bloom-in 重放 | 300ms | 花开缓动 |
| 花朵 hover | scale →1.08（花田格/选择器） | 150ms | ease-out |
| 茎高生长 | stemH 插值过渡 | 400ms | ease-out |
| 卡片 hover | shadow-1→shadow-2，无位移 | 180ms | ease-out |
| 页面切换 | fade + translateY(8px)→0 | 250ms | ease-out |
| Tooltip | fade + scale 0.96→1 | 120ms | ease-out |
| 弹窗/抽屉 | fade + translateY(12px)→0 | 220ms | ease-out |
| 学习行新增/删除 | 行高 0↔auto + fade（删除加 translateX(-8px)） | 200/180ms | ease-out/in |
| 保存指示 | 松绿点 scale 0.5→1 一次弹跳 | 200ms | ease-out |
| 新芽徽章出现 | scale 0.8→1 + fade | 250ms | 花开缓动 |
| 加载花开 | 花瓣 rotate/scale 展开 → 整体淡出 | 600ms | 花开缓动 |

---

## 7. 响应式设计

| 断点 | 布局 | 花田格 | 说明 |
|------|------|--------|------|
| 桌面 ≥1024px | 左侧导航 + 限宽内容 | min-h 96px，日期+花+茎 | 全量体验 |
| 平板 768~1023px | 顶部导航 + 全宽内容 | min-h 80px，花径缩小 | 花束海报转横排 |
| 手机 <768px | 底部标签导航 | min-h 52px，仅日期+花头（无茎） | 详情页全宽，弹窗转底部抽屉 |

- 移动端花田改「格间距 4px 独立格」（hairline 网格小屏过密）
- 心情选择器移动端 3×2 网格
- 今日花环、新芽徽章、空圆环全平台保留——它们是风格本体

---

## 8. 数据模型与存储

（与 v4.x 完全一致，**数据层零改动**。）

```typescript
interface DayRecord {
  id?: number;
  date: string;                   // YYYY-MM-DD，唯一索引
  mood: string | null;            // 内置 MoodType 或自定义心情 nanoid
  learnings: LearningItem[];      // { id, subject, durationMin, note, color }
  diary: string;                  // Markdown
  createdAt: number;
  updatedAt: number;
}
```

- `MoodConfig` 新增 `flower: string`（花名，如「向日葵金」）；自定义心情无此字段，UI 回退显示 label
- 连续天数、月度花束、时长→茎高映射均为**派生数据**，不落库
- 存储：IndexedDB + Dexie.js；日记防抖 1.5s；JSON 导入导出；均不变
- localStorage `study-journal-palette` 弃用（读取忽略即可，无需清理）

---

## 9. 技术架构与实现约定

### 9.1 技术栈
不变：React 18 + Vite + TypeScript + React Router v6；Tailwind CSS；Dexie.js；React Context；nanoid；Recharts（统计页可按需保留或手绘 SVG）；lucide-react；PWA。

**调整**：
- 字体：Google Fonts `Noto Serif SC`（600）+ `Fraunces`（italic 400/500）+ `IBM Plex Mono`（400/500）；移除 Caveat 与 Inter
- 新增 `src/components/Flower.tsx`：全应用唯一花卉生成组件（§4.1）
- 移除 `src/assets/moods/` 印章 SVG 体系（由 Flower 取代）
- `MoodConfig` 增 `flower` 字段；`SUBJECT_COLORS` 换花园色板
- 移除 `PaletteType` / `PALETTES` / `data-palette` 机制与设置页配色选项

### 9.2 Token 架构（沿用 v4.1 结论）
- 中性色/品牌色唯一来源 = `src/index.css` CSS 变量（`:root` 与 `.dark` 两块，**不再有 data-palette**）
- `tailwind.config.js` 颜色仅 `var()` 引用；心情色唯一来源 = `MOOD_CONFIGS`
- 新增令牌：`accent` / `accent-ink`（杏橙）、`font-display-latin`（Fraunces）、圆角 `xl:20 / 2xl:28`、动画 `bloom-in`

### 9.3 文件改造清单

| 文件 | 改造内容 | 量级 |
|------|----------|------|
| `src/index.css` | 变量块换晨园/夜色双色；删 data-palette；删 washi/stitched/title-tick；光斑背景；diary-paper 保留 | 重写 |
| `tailwind.config.js` | 字体（Fraunces 换 Caveat/Inter）、圆角、阴影、动画（bloom-in）对齐 v5.0 | 重写 |
| `src/lib/constants.ts` | MOOD_CONFIGS 换花园六色 + flower 花名；SUBJECT_COLORS 换色板；删 PALETTES | 重写 |
| `src/types/index.ts` | MoodConfig 增 flower；删 PaletteType | 小改 |
| `src/components/Flower.tsx` | **新建**：花朵生成器（§4.1） | 新建 |
| `src/lib/streak.ts` | **新建**：连续天数计算 | 新建 |
| `src/pages/TodayDetail.tsx` | 盖花 hero 卡 + 园丁清单 + 信纸 + 新芽徽章 + 每日一句 | 重写 |
| `src/components/MoodSelector.tsx` | 花朵选择器（tint 底花头 → solid 盖花） | 重写 |
| `src/components/LearningRecordCard.tsx` | 园丁清单行 | 重写 |
| `src/components/DiaryEditor.tsx` | 信纸视觉对齐（沿用逻辑） | 中改 |
| `src/components/Layout/Sidebar/BottomNav` | 花期导航（Logo/激活态/今日入口） | 重写 |
| `src/pages/MonthView.tsx` + `DateCard.tsx` | 花田网格 + 今日花环 + 空圆环 + tooltip + 图例 | 重写 |
| `src/pages/Memory.tsx` | 时光轴（花头标签 + 学科胶囊） | 大改 |
| `src/pages/Stats.tsx` | 摘要卡/柱图/环形图 + 月度花束海报（含 PNG 导出） | 大改 |
| `src/pages/Search/Settings/AnnualReview` | token 跟随；设置删配色项 | 中改 |
| `src/components/BookLoader.tsx` | 花开加载动画 | 重写 |
| `src/components/LearningFormModal/MoodEditModal` | 浮层规范对齐 | 小改 |
| `index.html` | 字体链接更换 | 小改 |

---

## 10. 实施状态总表

> ✅ 已实现　🚧 进行中　⬜ 未实现（随开发进度更新）

| 模块 | 内容 | 状态 |
|------|------|------|
| A. Token 层 | index.css / tailwind.config / constants / types / 字体 | ⬜ |
| A. 花朵生成器 | Flower.tsx + 茎高映射 | ⬜ |
| A. 连续天数 | lib/streak.ts | ⬜ |
| B. 今日页 | 页眉+新芽徽章+每日一句+盖花 hero+园丁清单+信纸 | ⬜ |
| B. 导航布局 | Layout / Sidebar / BottomNav | ⬜ |
| B. 学习弹窗 | LearningFormModal 视觉对齐 | ⬜ |
| C. 花田月视图 | 网格 / 今日花环 / 空圆环 / tooltip / 图例 | ⬜ |
| D. 时光轴 | Memory 页 | ⬜ |
| D. 统计页 | 摘要卡 / 柱图 / 环形图 | ⬜ |
| D. 月度花束 | 海报渲染 + PNG 导出分享 | ⬜ |
| E. 其余页面 | Search / Settings / AnnualReview | ⬜ |
| E. 加载动画 | BookLoader 花开 | ⬜ |
| E. 深色模式 | 全站走查 + 对比度验收 | ⬜ |
| E. 自定义心情弹窗 | MoodEditModal 对齐 | ⬜ |

---

## 11. 附录

### 11.1 设计资产
- 高保真设计稿：`design/mockup.html`（含花朵生成器参考实现）
- 效果示意图：`design/shots/{board,today,garden,timeline,stats,dark,mobile}.png`
- 截图脚本：`design/shot.mjs`

### 11.2 设计灵感
- **生长机制**：GitHub 贡献图的诗意化；旅行青蛙/Forest 的「种出来」回访动机
- **花园气质**：MUJI 的克制、Kinfolk 的留白、日系杂货的柔色
- **排版**：Monocle 杂志中西混排节奏；Fraunces 的「旧式新排」衬线气质

### 11.3 文案语气（Tone of Voice）
- 把「记录」说成「种/盖花」，把「坚持」说成「生长」，但不滥情：界面正文仍以清晰为先
- 示例：「把时间种下去，就会开花。」「疲惫的日子也要记录，花照样开。」「月末，把花田扎成一束。」

### 11.4 文档版本演进
- **v1.0 ~ v3.3**：定位与功能确立；视觉为「柔彩果冻风」
- **v4.0 / v4.1**：纸上手帐·编辑风（印章/纸纹/三配色主题）——已被本文档取代
- **v5.0 花期 Blossom（本文档）**：核心隐喻改为「记录=开花」的生长叙事；单签名配色（晨雾白×深叶绿×杏橙）；心情印章体系改为程序化花朵生成器；月历改为花田；新增新芽徽章（连续天数）与月度花束海报（PNG 分享）；移除多配色主题、和纸胶带、缝线、题字竖线；数据层零改动

---

**文档版本**：v5.0 花期 Blossom
**最后更新**：2026年7月26日
**文档状态**：实施中（进度见 §10）
